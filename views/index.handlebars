<!-- burgers to devour -->
      <div class="row mt-3">
        <div class="col-6">
            <h3>Burgers to Devour</h3>
            <ul class="list-group" id="burgers">
                 {{#each burger}}
                 {{#unless devoured}}
                 {{> burgers/burger-block devoured=false}}
                 {{/unless}}
                 {{/each}}
            </ul>
        </div>